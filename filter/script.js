var vec = [3,4,2,6,7,5];

//Aplica a todos los elementos como el map pero solo trae los que satisfacen la expresion
// console.log(vec.filter(x => x % 2 == 0));

console.log(empleados.filter(x => x.genero === "Male"));
//cuando volvi el profesor habia hecho esto con === en vez de == ni idea por que, pero funciona de las dos formas en este caso